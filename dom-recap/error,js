// not in js that frequently: range error
const numbers =[3, 4, 5,8];
// you should not do it (most of time)
numbers.length = 2;
console.log(numbers);
console.log(numbers.length);
// console.log(numbers[25]);

// reference error
//  we won't use var as it won't give error , will give undefined
// whisting
// console.log(money);
const money = 80;

// syntax error
// for(let i = 0; i < 5 i++){

// }

// if(numbers.length >/2){

// }

// typeError (cannot read properties of undefined)
// let student = { address:{city: 'dhaka'}};
let student ={address: 'japan'}
    // console.log(student.name);
    // console.log(student.address.city)

    // error construction
    // we can make error with massage
